#include <algorithm> //набор функций для операций над контейнерами и последовательностями
#include <cstddef> // определяет макросы NULL и оffsetoff и типы ptrdiff_t, wchar_t, size_t;
#include <iterator> // объект который может перебирать элементы в кнтейнере стандартной библиотеки с++ и предоставлять доступ к отделным элементам
#include <memory> // определяет класс, оператор и несколько шаблонов, позволяющих выделять и освобождаь объекты

template<typename T> //шаблон
